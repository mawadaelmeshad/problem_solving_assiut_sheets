#include <iostream>
#include <string>

using namespace std;

int main()
{
   int n,sum,num;
   sum=0;
   cin>>n;
   string s;
    cin>>s;
   for(int i=0;i<n;i++)
   {
       num= int(s[i]-'0');
       sum=num+sum;
   }
   cout<<sum;
    return 0;
}
