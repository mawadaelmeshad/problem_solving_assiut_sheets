#include <iostream>
#include <cstdlib>
using namespace std;

int main()
{
  long long n,i,sum;
    sum=0;
    cin>>n;
  long long  k[n];
    for ( i=0;i<n;i++)
    {
        cin>>k[i];
        sum=sum+k[i];
    }
    cout<<abs(sum);
    return 0;
}
