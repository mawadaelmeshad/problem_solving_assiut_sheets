#include <iostream>
using namespace std;

int main()
{
 int n; cin>>n;
 int right=n%10;
 int left= n/10;
 if((left ==0 || right==0)|| (left%right==0 || right%left==0))
    cout<<"YES";
 else
    cout<<"NO";
    return 0;
}
